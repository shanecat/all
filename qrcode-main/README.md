# 交貨便代碼 QR Code 批次工具

這是一個靜態網頁工具，可把多筆交貨便代碼轉成 QR Code。每一行代表一個代碼，產生後一次只顯示一張 QR Code，方便用 7-11 App 逐張掃描。

## 使用方式

1. 開啟 `index.html`。
2. 把交貨便代碼貼到文字框中，每行一筆。
3. 按「產生 QR Code」。
4. 用「上一張／下一張」切換要掃描的代碼。

頁面也支援鍵盤操作：按左右方向鍵切換，按空白鍵前往下一張。

## 隱私

代碼只會在瀏覽器中處理，不會寫入 Google 試算表或資料庫。

QR Code 產生器已放在本機檔案中，不需要連外部 CDN。
